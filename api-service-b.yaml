apiVersion: v1
kind: Service
metadata:
  name: db-api
  labels:
    app: redis
spec:
  type: ClusterIP
  selector:
    app: redis
  ports:
    - port: 8000
      targetPort: 8000
      name: http
      protocol: TCP